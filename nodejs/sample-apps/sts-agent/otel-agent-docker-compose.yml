---
version: '3'

services:
  stackstate-agent:
    image: "docker.io/stackstate/stackstate-agent-2-test:${AGENT_BRANCH}"
    privileged: true
    ports:
      - "8126:8126"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "/proc/:/host/proc/:ro"
      - "/sys/fs/cgroup/:/host/sys/fs/cgroup:ro"
      - "/etc/passwd:/etc/passwd:ro"
      - "/sys/kernel/debug:/sys/kernel/debug"
    environment:
      STS_API_KEY: "${STS_API_KEY}"
      STS_STS_URL: "${AGENT_ENDPOINT}:7070/stsAgent"
      STS_PROCESS_AGENT_URL: "${AGENT_ENDPOINT}:7077/stsAgent"
      STS_APM_URL: "${AGENT_ENDPOINT}:7077/stsAgent"
      STS_PROCESS_AGENT_ENABLED: "true"
      STS_NETWORK_TRACING_ENABLED: "true"
      STS_APM_ENABLED: "true"
      HOST_PROC: "/host/proc"
      HOST_SYS: "/host/sys"
